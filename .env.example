# ===========================================
# THE UNSAID - Environment Variables Template
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control
#
# Variable Naming Convention:
# - PUBLIC_* = Safe to expose to browser (used by SvelteKit frontend)
# - PRIVATE_* = Server-side only, never exposed to browser
# - No prefix = Backend-only variables

# ------------------------------------------
# SUPABASE (Frontend + Backend - REQUIRED)
# ------------------------------------------
# Get these from: https://supabase.com/dashboard/project/_/settings/api

# Supabase project URL (used by both frontend and backend)
PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anon/publishable key (safe for frontend)
PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key-here

# Supabase service role key (backend only - NEVER expose to frontend)
PRIVATE_SUPABASE_SECRET_KEY=your-secret-key-here

# ------------------------------------------
# OPENROUTER (Backend AI Processing - REQUIRED)
# ------------------------------------------
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key-here

# Optional: Default model for AI requests
# See https://openrouter.ai/models for available models
OPENROUTER_DEFAULT_MODEL=google/gemini-3-flash-preview

# ------------------------------------------
# APPLICATION URLs (REQUIRED for production)
# ------------------------------------------
# Frontend URL - used for CORS and API referer header
# For local development: http://localhost:5173
# For Vercel: https://your-app.vercel.app
PUBLIC_APP_URL=http://localhost:5173

# Backend API URL - where the FastAPI backend is hosted
# For local development: http://localhost:8000
# For production: https://your-api-domain.com
PUBLIC_API_URL=http://localhost:8000

# ------------------------------------------
# RATE LIMITING (Backend)
# ------------------------------------------
# AI requests per user per hour (default: 10)
AI_RATE_LIMIT_PER_HOUR=15

# ------------------------------------------
# ENVIRONMENT MODE
# ------------------------------------------
# Set to 'development' or 'production'
# Controls CORS origins and debug features
NODE_ENV=development

# ------------------------------------------
# ENCRYPTION (Reference Only)
# ------------------------------------------
# These are informational - actual key derivation happens client-side
# PBKDF2 iterations (higher = more secure but slower)
# Recommended: 100000 for production
PBKDF2_ITERATIONS=100000

# ------------------------------------------
# VERCEL DEPLOYMENT
# ------------------------------------------
# When deploying to Vercel, set these in the Vercel dashboard:
# Environment Variables > Add the following:
#
# REQUIRED:
#   PUBLIC_SUPABASE_URL
#   PUBLIC_SUPABASE_PUBLISHABLE_KEY
#   PRIVATE_SUPABASE_SECRET_KEY
#   PUBLIC_APP_URL (set to your Vercel domain)
#   PUBLIC_API_URL (set to your backend API URL)
#
# For Backend (if using Vercel for backend):
#   OPENROUTER_API_KEY
#   OPENROUTER_DEFAULT_MODEL (optional)
#   AI_RATE_LIMIT_PER_HOUR (optional, default: 10)
#   NODE_ENV=production
